import { Account } from './Account';

export class UserDetails {
    name: string; 
    userId : String;
    email: string;
    password: string;
    phoneNumber: string;
    familyName: string;
    accounts: Account[]; 
    }
